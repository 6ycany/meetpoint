# ROADMAP.md — プロジェクト進行手順・ロードマップ

> **このファイルの使い方**: 新しいプロジェクトを始めるとき・進め方に迷ったときに参照する。
> フェーズごとの「何をするか」「どのファイルを使うか」「Claude にどう伝えるか」をまとめた手順書。

---

## プロジェクトの全体フロー

```
[Phase 0] セットアップ       所要: 5分
    ↓
[Phase 1] 音声ダンプ         所要: 10〜15分
    ↓
[Phase 2] 仕様壁打ち         所要: 15〜30分
    ↓
[Phase 3] 実装               所要: タスクによる
    ↓
[Phase 4] 検証・ドライラン   所要: 30分〜
    ↓
[Phase 5] 定常運用化         所要: 30分
    ↓
[継続]    ナレッジ蓄積ループ  毎セッションごと
```

---

## Phase 0 — セットアップ（初回のみ）

**目的**: プロジェクトディレクトリを作り、作業できる状態にする

### やること

```bash
# モノレポのルートから初期化スクリプトを実行
./new-project.sh <プロジェクト名> [once|monthly|agent]

# 例
./new-project.sh 経費精算 monthly
./new-project.sh 提案書-ABC社 once
./new-project.sh メールリマインダー agent
```

### 確認ポイント

- [ ] 6つのファイルが生成されている（HANDOFF / CLAUDE / PLAN / SPEC / TODO / KNOWLEDGE）
- [ ] `data/` `output/` `scripts/` ディレクトリがある
- [ ] `CLAUDE.md` に音声入力ルールが記載されている

---

## Phase 1 — 音声ダンプ（PLAN.md を埋める）

**目的**: 頭の中にあることを全部 PLAN.md に出力する。整理は不要。

### やること

1. `PLAN.md` を開く
2. **音声入力で10分くらい話す**（以下のことを気にせず喋る）
   - なぜこの作業を自動化したいか
   - 今どんな手順でやっているか（現状フロー）
   - どこが一番つらいか
   - 理想のゴール
   - 制約・前提（使えないツール、権限がないAPIなど）
3. そのまま PLAN.md に貼り付ける

### ポイント

> 「えっと〜」「やっぱりそうじゃなくて〜」のフィラーはそのままでいい。
> Claude が汲み取ってくれる。整理しようとすると情報が減る。

### Phase 1 終了の目安

PLAN.md に **500文字以上**の生テキストが入っていればOK。

---

## Phase 2 — 仕様壁打ち（SPEC.md を作る）

**目的**: Claude と認識を合わせ、「何をやるか / やらないか」を確定する。

### Claude Code での進め方

```bash
cd tasks/<プロジェクトディレクトリ>
claude
```

**セッション開始の第一声（コピペ用）**:

```
PLAN.md を読んで、内容を整理した上で SPEC.md のドラフトを作成してください。
特に「スコープイン / スコープアウト」「インプット / アウトプット」「完了条件」を明確にしてください。
```

### 壁打ちのチェックリスト

Claude が SPEC.md を出したら、以下を確認する：

- [ ] **インプット**: 何のデータを使うか（ファイル、API、手入力）
- [ ] **アウトプット**: 何が生成されるか（形式・保存先）
- [ ] **スコープアウト**: やらないことが明記されているか
- [ ] **完了条件**: 「これができたら成功」が具体的か
- [ ] **制約**: 使えないAPI・権限・環境の制限が反映されているか

### Phase 2 終了の目安

SPEC.md の「完了条件」にチェックボックスが3つ以上あり、Claude と「これでいきましょう」と合意できたらOK。

---

## Phase 3 — 実装

**目的**: SPEC.md を元に Claude Code と一緒に作業を進める。

### セッション開始の第一声（コピペ用）

```
HANDOFF.md と KNOWLEDGE.md の最新エントリを読んで現状を把握してください。
TODO.md の未完了タスクを確認して、次に着手すべきものを提案してください。
```

### 実装中のルール

**タスクの粒度**
: TODO.md のタスクは「30分以内で完了できる」単位にする。大きい場合は Claude に分解してもらう。

**こまめに TODO.md を更新**
: タスク完了のたびにステータスを更新する（`⬜ → ✅`）。コンテキストをリセットしても再開できる状態を保つ。

**ハマったらすぐ KNOWLEDGE.md に書く**
: 「あとで書こう」は書かない。解決した瞬間に記録する。

### セッション中断（コンテキストリセット）前にやること

Claude に伝える：

```
作業を中断します。
今セッションの作業内容を要約して KNOWLEDGE.md に追記してください。
HANDOFF.md の「現在の実装状況」と「次のタスク」も最新の状態に更新してください。
```

### セッション再開の第一声（コピペ用）

```
HANDOFF.md と KNOWLEDGE.md の最新エントリを読んで、前回の続きから始めてください。
```

---

## Phase 4 — 検証・ドライラン

**目的**: 実際のデータで動かし、問題を洗い出す。

### やること

1. `data/` に実際の入力データを置く
2. Claude に実行してもらう
3. `output/` の結果を確認する
4. ズレがあれば SPEC.md に差分を記録する

### 確認のポイント

- [ ] SPEC.md の完了条件が全て満たされているか
- [ ] エラー・例外が適切に処理されているか
- [ ] 出力フォーマットが実際の使用に耐えるか（コピペしやすいか、等）
- [ ] 月次タスクの場合、来月も同じ手順で動くか

### ドライランで問題が出たら

KNOWLEDGE.md に記録 → SPEC.md を更新 → Phase 3 に戻る。

---

## Phase 5 — 定常運用化

**目的**: 「来月また最初からやり直し」にならないよう、手順を固める。

### やること

**README.md（プロジェクト内）を作成する**

Claude に依頼：

```
このプロジェクトの作業手順を README.md にまとめてください。
「次回この作業をするとき、自分だけで最初から再現できる」レベルの手順書にしてください。
```

README.md に含める内容：
- 作業の概要（何のために / 何を自動化しているか）
- 実行手順（コマンド・操作を順番に）
- 入力データの準備方法
- 出力結果の使い方（どこにコピペするか、等）
- 注意点（KNOWLEDGE.md の重要エントリを転記）

**月次タスクの場合は当月エントリを作る**

```bash
# monthly プロジェクトの当月作業メモ
mkdir -p tasks/毎月-<名前>/$(date +%Y-%m)
```

---

## 継続ループ — ナレッジ蓄積

**目的**: 使うたびに賢くなる。属人化ではなく、リポジトリに知識を蓄積する。

### セッションごとのルーティン

```
作業開始 → HANDOFF.md + KNOWLEDGE.md を読む
作業中   → TODO.md を更新しながら進める
作業中断 → KNOWLEDGE.md に追記 + HANDOFF.md を更新
翌月     → KNOWLEDGE.md の最新エントリを読んで再開
```

### ナレッジが溜まってきたらやること

**snippets/ への昇格**
: 「他のプロジェクトでも使えそう」なスクリプトは `snippets/` にコピーする。

**モノレポ横断検索**
: 「あの作業のときどうやったっけ？」はリポジトリルートで Claude に聞くだけ。

```bash
cd ~/work   # モノレポルート
claude
# → 「経費精算のCSV解析のときに使った文字コード処理、教えて」と聞く
```

---

## ドキュメント更新タイミング一覧

| ファイル | 更新タイミング |
|----------|--------------|
| `PLAN.md` | Phase 1（一度書いたら基本変えない） |
| `SPEC.md` | Phase 2・仕様変更時 |
| `TODO.md` | タスク完了のたびに |
| `KNOWLEDGE.md` | ハマった瞬間・セッション終了時 |
| `HANDOFF.md` | セッション終了時（必ず最新に保つ） |
| `CLAUDE.md` | プロジェクト固有ルールが増えたとき |
| `README.md` | Phase 5（初回）・手順変更時 |

---

## よくある詰まりポイントと対処

### 「SPEC.md が広すぎて作業が終わらない」

→ スコープアウトを Claude に追加してもらう。「一番つらい部分だけ」を残す。

### 「Claude が前のセッションの内容を忘れた」

→ HANDOFF.md と KNOWLEDGE.md を更新していれば問題なし。「HANDOFF.md を読んで」と伝えるだけで再開できる。

### 「出力結果がイメージと違う」

→ SPEC.md の「完了条件」が曖昧な証拠。具体例（before/after のサンプル）を SPEC.md に追加してから再実行。

### 「MCPツールがうまく動かない」

→ `~/.claude/claude.json` の設定を確認。KNOWLEDGE.md に解決策を記録しておく。

### 「毎月同じ作業で毎回ハマる」

→ KNOWLEDGE.md の「教訓」を `HANDOFF.md` の「既知の問題・注意点」にも転記する。セッション開始時に必ず目に入るようにする。

---

*このロードマップは実際に使いながら改善する。「このフェーズ、自分には合わない」と思ったら CLAUDE.md に書いて Claude に伝える。*
